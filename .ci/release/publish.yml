steps:
  - task: PublishBuildArtifacts@1
    displayName: "Publish ${{ parameters.platform }}"
    inputs:
      pathToPublish: "re-log-ppx/lib/bs/native/logppx.native${{ parameters.fileEnding }}"
      artifactName: "${{ parameters.platform }}"
      parallel: true
      parallelCount: 8
