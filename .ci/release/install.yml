steps:
  - task: NodeTool@0
    displayName: "Use Node 8.x"
    inputs:
      versionSpec: 8.x

  - script: yarn install
    workingDirectory: "bs-log"
    displayName: "bs-log: yarn install"

  - script: yarn install
    workingDirectory: "bs-log-ppx"
    displayName: "bs-log-ppx: yarn install"
